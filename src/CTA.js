import React, { Component } from 'react';

class CTA extends Component {
  render() {
    return (
      <button class="button">
        Learn more
      </button>
    )
  }
}

export default CTA; // Don’t forget to use export default!